import { assertEquals } from "@std/assert";

import { FindElements } from "./main.ts";

Deno.test.ignore(function numTilePossibilitiesTest() {
  // const elements = new FindElements([-1, null, -1, -1, null, -1]);

  new FindElements([-1,-1,-1,-1,-1]);
});
